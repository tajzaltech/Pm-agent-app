"use client";

import SourcesPage from "@/app/(app)/sources/page";

/**
 * Connections is the canonical workspace for the complete
 * Sources → Repos → Outputs setup and management flow.
 */
export default function ConnectionsPage() {
  return <SourcesPage embedded />;
}
